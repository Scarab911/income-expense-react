import { makeStyles } from "@material-ui/core";

export default makeStyles((title) => ({
    income: {
        borderBottom: '5px solid #3D32FF',
    },
    expense: {
        borderBottom: '5px solid #FF2B44',
    }
}));